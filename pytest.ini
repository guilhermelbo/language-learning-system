[pytest]
testpaths = tests, backend/tests, frontend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --cov=backend/src --cov-report=html --cov-report=term-missing
cov_source = backend/src
filterwarnings =
    ignore::DeprecationWarning
